spring:
  application:
    name: micro-user
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/micro_user?serverTimezone=GMT
    username: root
    password: 123456
server:
  port: 60002
logging:
  level:
    com.alibaba.nacos.client.config.impl: WARN

micro:
  # jwt配置
  jwt:
    # 密钥
    key: micro
    # 过期时间（秒）
    expireTime: 7200
    # 校验时间（毫秒）
    validateTime: 300000